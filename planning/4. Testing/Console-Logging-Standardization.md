# Console Logging Standardization Plan

Console logging standardization turns out to be a terrible idea.  Much better to use inline console messages becuase that way the console logs tell you what file the messages is from.  With centralized logging, the console logs always refer to the console logging file, which is much less useful.